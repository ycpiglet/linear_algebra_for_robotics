schema_version: 1
id: PUB-008
title: rendered web·접근성·출판 품질 matrix
area: ci
status: planned
priority: P1
change_class: toolchain
version_impact: additive
owner_role: release-engineer
review_roles: [design-system-steward, render-qa]
branch: null
dependencies: [PUB-007]
decision_refs:
  - platform/design/README.md#검증-계층
decision_needed: []
blocker: null
status_reason: null
superseded_by: null
scope:
  in:
    - canonical web·DOM·a11y·keyboard·overflow gate
    - component specimen crop 중심 visual regression
    - 관련 변경의 PDF·EPUB build·검증 lane
  out:
    - 전 페이지 full screenshot baseline
    - CI의 자동 visual baseline 승인
change_paths:
  read:
    - Makefile
    - .github/workflows/**
    - design-system.qmd
    - scripts/verify_outputs.py
  write:
    - Makefile
    - .github/workflows/**
    - browser·visual·publication test와 승인 baseline
  exclusive_locks: [ci-control-plane]
  generated: [_site/**, _book/**, _proof/**, test-results/**]
verification:
  source_contract:
    - make test
    - make lint
    - path-impact matrix와 ambiguous fallback test
  rendered_contract:
    - 320·390·1280 px light/dark component crops
    - keyboard·overflow·automated accessibility smoke
    - PDF·EPUB font·link·reflow·static fallback
  post_merge:
    - main과 release candidate에서 full matrix
rollback:
  triggers:
    - visual noise가 실제 회귀보다 많음
    - path filter가 필요한 gate를 건너뜀
  unit: merge_commit
  steps:
    - 새 rendered lane만 advisory로 낮추거나 merge commit을 revert한다
    - 기존 test·lint·output verifier는 유지한다
  compatibility_preserved:
    - baseline update와 check command 분리
definition_of_done:
  - 이후 artifact migration을 보호할 stable rendered contract가 있다
  - deploy가 검증된 build artifact만 사용한다
  - baseline diff마다 사람 승인 evidence가 필요하다
evidence:
  issue: null
  pr: null
  ci_runs: []
  rendered_artifacts: []
  post_merge: []
  last_known_good: null
  revert_ref: null
  follow_up: null
last_reviewed: "2026-07-20"
